name: CI/CD for riverpod_di

on:
  workflow_dispatch:
  push: &trigger
    branches:
      - "**"
    paths:
      - "packages/riverpod_di/**"
      - ".github/workflows/riverpod_di_ci.yaml"
      - ".github/workflows/riverpod_di_cd.yaml"
  pull_request: *trigger

jobs:
  ci:
    name: CI
    uses: Skycoder42/dart_test_tools/.github/workflows/dart.yml@main
    with:
      workingDirectory: packages/riverpod_di
      buildRunner: false
      unitTestPaths: ""

  cd:
    name: CD
    uses: Skycoder42/dart_test_tools/.github/workflows/release.yml@main
    needs:
      - ci
    permissions:
      contents: write
      actions: write
    with:
      workingDirectory: packages/riverpod_di
      tagPrefix: riverpod_di-v
      publishWorkflow: riverpod_di_cd.yaml
